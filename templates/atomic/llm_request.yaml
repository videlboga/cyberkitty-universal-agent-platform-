# 🧩 АТОМАРНЫЙ ШАБЛОН: LLM ЗАПРОС
# Надежный блок для взаимодействия с языковыми моделями

atomic_template:
  type: "llm_request"
  name: "LLM запрос"
  version: "1.0.0"
  description: "Отправляет запрос к языковой модели и возвращает ответ"
  
  # Схема конфигурации
  config_schema:
    prompt:                 # string, required - Промпт для LLM
      type: "string"
      required: true
      description: "Текст запроса к LLM, поддерживает шаблоны {var}"
      
    model:                  # string, optional - Модель LLM
      type: "string"
      default: "deepseek/deepseek-chat"
      description: "Название модели LLM"
      
    max_tokens:             # integer, optional - Максимум токенов
      type: "integer"
      default: 1000
      description: "Максимальное количество токенов в ответе"
      
    temperature:            # float, optional - Креативность
      type: "float"
      default: 0.7
      description: "Температура модели (0.0-1.0)"
      
    system_prompt:          # string, optional - Системный промпт
      type: "string"
      default: ""
      description: "Системный промпт для модели"
      
    response_format:        # string, optional - Формат ответа
      type: "string"
      default: "text"
      enum: ["text", "json"]
      description: "Формат ответа от LLM"
      
    save_to_context:        # string, optional - Ключ для сохранения
      type: "string"
      default: "llm_response"
      description: "Ключ в контексте для сохранения ответа"

  # Пример использования
  example:
    type: "llm_request"
    config:
      prompt: "Пользователь спросил: {user_question}. Ответь кратко и по делу."
      model: "deepseek/deepseek-chat"
      max_tokens: 500
      temperature: 0.7
      system_prompt: "Ты полезный ассистент. Отвечай на русском языке."
      save_to_context: "assistant_reply"

  # Поведение
  behavior:
    generates_buttons: false          # Не создает кнопки
    expected_event_type: "any"        # Принимает любое событие
    response_type: "immediate"        # Отвечает мгновенно
    state_change: "always"            # Всегда переходит к next_step
    external_api: true                # Использует внешний API
    
  # Контекст, который создается
  context_updates:
    "{save_to_context}": "Ответ от LLM"
    "last_llm_model": "Использованная модель"
    "last_llm_tokens": "Количество потраченных токенов"
    
  # Возможные ошибки
  error_cases:
    - api_error: "Ошибка API LLM провайдера"
    - timeout: "Превышено время ожидания ответа"
    - invalid_model: "Неподдерживаемая модель"
    - token_limit: "Превышен лимит токенов"

# Реализация для простой state machine
simple_implementation: |
  class LLMStep(BaseStep):
      async def execute(self, event: Event, state: UserState) -> StepResult:
          try:
              # Подготавливаем промпт
              prompt = self.resolve_template(self.config["prompt"], state.context)
              
              # Параметры для LLM
              llm_params = {
                  "model": self.config.get("model", "deepseek/deepseek-chat"),
                  "max_tokens": self.config.get("max_tokens", 1000),
                  "temperature": self.config.get("temperature", 0.7)
              }
              
              # Вызываем LLM (здесь должен быть реальный API вызов)
              llm_response = await self.call_llm_api(prompt, llm_params)
              
              # Сохраняем в контекст
              save_key = self.config.get("save_to_context", "llm_response")
              context_updates = {
                  save_key: llm_response,
                  "last_llm_model": llm_params["model"],
                  "last_llm_tokens": len(llm_response.split())  # Примерная оценка
              }
              
              return StepResult(
                  response=f"🤖 {llm_response}",
                  next_step=self.config.get("next_step"),
                  update_context=context_updates
              )
              
          except Exception as e:
              logger.error(f"LLM ошибка: {e}")
              return StepResult(
                  response="❌ Ошибка при обращении к LLM. Попробуйте позже.",
                  next_step=self.config.get("error_step", self.config.get("next_step"))
              ) 