# 🧩 АТОМАРНЫЙ ШАБЛОН: ПРОСТОЕ МЕНЮ
# Надежный, тестированный блок для отображения кнопок и обработки выбора

atomic_template:
  type: "menu"
  name: "Простое меню"
  version: "1.0.0"
  description: "Отображает текст с кнопками и ждет выбор пользователя"
  
  # Схема конфигурации
  config_schema:
    text:                   # string, required - Текст сообщения
      type: "string"
      required: true
      description: "Текст сообщения перед кнопками"
      
    choices:                # object, required - Варианты выбора
      type: "object" 
      required: true
      description: "Объект с вариантами: choice_id -> {text, next_step}"
      
    allow_any_input:        # boolean, optional - Принимать любой ввод
      type: "boolean"
      default: false
      description: "Если true, принимает любой callback, не только из choices"

  # Пример использования
  example:
    type: "menu"
    config:
      text: "Выберите действие:"
      choices:
        "test_llm":
          text: "🤖 Тест LLM"
          next_step: "llm_step"
        "test_rag":
          text: "📚 Тест RAG"
          next_step: "rag_step"
        "exit":
          text: "🚪 Выход"
          next_step: "end_step"
      allow_any_input: false

  # Поведение
  behavior:
    generates_buttons: true           # Создает Telegram кнопки
    expected_event_type: "callback"   # Ожидает callback_query
    response_type: "immediate"        # Отвечает мгновенно
    state_change: "conditional"       # Меняет состояние только при валидном выборе
    
  # Контекст, который создается
  context_updates:
    last_choice: "ID выбранного варианта"
    last_choice_text: "Текст выбранного варианта"
    
  # Возможные ошибки
  error_cases:
    - invalid_choice: "Выбор не найден в choices"
    - missing_config: "Отсутствует text или choices"
    - wrong_event_type: "Получен не callback"

# Реализация для простой state machine
simple_implementation: |
  class MenuStep(BaseStep):
      async def execute(self, event: Event, state: UserState) -> StepResult:
          if event.type != "callback":
              # Показываем меню
              text = self.resolve_template(self.config["text"], state.context)
              buttons = []
              for choice_id, choice_config in self.config["choices"].items():
                  buttons.append({
                      "text": choice_config["text"],
                      "callback_data": choice_id
                  })
              return StepResult(response=text, buttons=buttons, next_step=state.current_step)
          
          # Обрабатываем выбор
          choice = event.data
          if choice not in self.config["choices"]:
              if not self.config.get("allow_any_input", False):
                  return StepResult(response="❌ Неверный выбор", next_step=state.current_step)
          
          choice_config = self.config["choices"].get(choice, {"text": choice})
          return StepResult(
              next_step=choice_config.get("next_step"),
              update_context={"last_choice": choice, "last_choice_text": choice_config.get("text", choice)}
          ) 